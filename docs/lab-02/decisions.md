# TokTickIT Lab 2 - Approved Decision Log

Status: **Approved**. These decisions are settled and binding on
`specification.md`, `api-spec.md`, `ui-spec.md` and `tests.md`. Where the labsheet
leaves a choice open, the decision below is the choice. Where the labsheet
contradicts itself, the decision below is the resolution.

Owner: Tammathorn Kananurak (67070503489). Lab 2, Sprint 2.

Labsheet references are to `docs/lab-02/spec/Lab_02_labsheet.pdf`.

## Decision table

| ID | Area | Decision | Rationale | Labsheet reference |
|---|---|---|---|---|
| C-01 | Project guardrail | `CLAUDE.md` is rewritten as a Lab 2 document before any specification work begins. The Lab 1 prohibitions on Playwright, on tickets/uploads/attachments/new screens, and on new top-level directories are removed. | The Lab 1 file forbids most of the Lab 2 deliverable, and its own opening states that its rules override technical preference. A guardrail that contradicts the sprint produces either refused work or silently broken rules, and the file is graded evidence in Part 1. | 12, 8.8, 9.2, 4.2 |
| C-02 | Test file names | Section 12 file names are authoritative everywhere. Section 9.1 disagrees three times - `tickets.api.test.ts`, `e2e/lab-02/create-ticket.spec.ts`, and `client/src/.../CreateTicket.test.tsx` - and is disregarded in all three. | Section 12 is the repository tree that Part 1 is graded against. Section 9.1 is captioned as an illustration and uses placeholder `...` path segments. | 9.1, 12 |
| C-03 | Client test path | Client Lab 2 tests live in `client/tests/lab-02/`. | `client/vite.config.ts` sets `include: ["tests/**/*.test.tsx"]`, rooted at `client/`. Any location outside that glob collects zero tests and still reports success, which would hide missing coverage behind a green run. `client/tests/lab-02/` also matches the existing `client/tests/lab-01/` convention and needs no configuration change. | 12, 9.1 |
| C-04 | Lab 1 regression | The Lab 1 Check System screen is preserved as a `SystemCheck` component reachable at a route, and `client/tests/lab-01/App.test.tsx` is repointed at that component. No Lab 1 test is deleted. | The Lab 1 test renders `<App />` and asserts a Check System button and the Online/Offline states. Lab 2 replaces the root screen, so those assertions would fail. The Definition of Done requires all tests to pass on the final `main`, and removing tests to achieve a green suite is not an acceptable route to it. | 13.1 |
| C-05 | Reference data | `Category` and `RelatedSystem` both carry `isActive Boolean @default(true)`. `GET /api/categories` keeps returning the same `{id, name}` shape in id order. | Section 6 requires endpoints that retrieve *active* Categories and *active* Related Systems, which presumes the flag. The Lab 1 supertest maps only `name`, so an added column and an active filter leave it passing. This also supplies the optional-field and index discussion Section 5.2 requires. | 6, 5.2, 5.3 |
| C-06 | Screen modes | Lab 2 implements view and create modes only. Edit mode is identified and explicitly deferred, and is recorded as such in `specification.md` Section 11. | Section 8.6 asks the modes to be identified, not necessarily all implemented. Section 8.5 requires Ticket Detail header fields to be read-only and Section 4.2 excludes status changes, so an edit mode would have nothing legitimate to edit. | 8.5, 8.6, 11.2 |
| C-07 | IT Priority | `Ticket.itPriority` exists as a nullable column. It is never settable in Lab 2 and its badge renders only when a value is present. | Section 8.8 requires badge consistency across Requested Priority, IT Priority and Current Status, while Section 4.2 excludes changing IT Priority as an IT Staff function. A read-only nullable column satisfies the visual requirement without implementing staff workflow, and gives Lab 3 a place to land. | 8.8, 4.2 |
| C-08 | Unit test location | A dedicated `server/tests/lab-02/ticket-number.unit.test.ts` holds the unit-level tests. | Section 9.2 mandates a unit level and names the Ticket Number generator as its example, but Section 12 lists only API test files. Section 12 is a stated minimum, so additions are permitted. A generator test needs no database and is the cheapest evidence in the sprint. | 9.2, 12 |
| C-09 | UI style and responsive levels | UI-style assertions live inside the four named client test files. Responsive evidence lives in the single E2E spec. The test level is carried by the Test ID column, not by the file name. | Section 9.2 requires six distinct levels while Section 12 fixes the client file set. Placing the assertions inside the named files fills every level without disturbing the graded tree. | 9.2, 8.8, 12 |
| C-10 | Playwright configuration | Chromium only, with three viewport projects: desktop 1280, tablet 834, mobile 390. Screenshots are written explicitly into `artifacts/lab-02/screenshots/{create-ticket,my-tickets,ticket-detail}/`. `webServer` stays disabled and the manual start sequence is documented in `tests.md`. `artifacts/` is tracked; `test-results/`, `playwright-report/` and `blob-report/` stay ignored. | The marks are for viewport behavior, not for a browser matrix; three browsers would triple runtime and screenshot volume for no marks. The API also requires the Docker Postgres container, so an automatic `webServer` would fail in a way that looks like a test failure. | 8.8, 12 |
| C-11 | Ticket Number format | Format is `TKT-YYYY-NNNNNN`, for example `TKT-2026-000042`. The six digits are the row's autoincrement id, zero-padded. The year is taken from the ticket's `createdAt`. This is not a per-year counter, so there is no race and no sequence table. The unit test asserts `^TKT-\d{4}-\d{6}$`. | The format matches the Ticket Numbers shown in the labsheet's own approved illustrations on pages 2 and 11, and Section 8.8 requires visual checking against those illustrations rather than against memory. Deriving the digits from the autoincrement id keeps generation collision-free without a counter table, and the year remains readable to a human. | 4.3 BR-01, 9.2, 8.8, illustrations pp. 2 and 11 |
| C-12 | Requester identity transport | `requesterId` travels in the POST body for ticket creation and as a `?requesterId=` query parameter on GET endpoints. No custom header. | This is the only form the labsheet actually shows, so the contract stays traceable to it. Keeping the identifier visible in the request rather than in an auth-shaped header reinforces that the selector is a testing mechanism, which is the point of BR-03. | 6.2, 4.3 BR-03 |
| C-13 | Ownership failure status | 403 when the Ticket or Attachment exists but belongs to another Requester. 404 when it does not exist at all. This deliberately leaks the existence of another Requester's records. The trade-off is accepted because Lab 2 has no authentication at all per Section 4.2, so there is no security boundary to protect; Lab 3 may revisit the choice once real authentication exists. Knock-on to C-20: a removed file returns 410 Gone to the owner and 403 to a non-owner. | Section 6.4 lists missing resources and ownership failure as separate statuses to be identified, so collapsing both into 404 leaves that requirement unanswered. Part 8 requires a readable screenshot of a rejection, and 403 Access denied reads as a deliberate refusal where 404 reads as a mistyped URL. | 6.3, 6.4, 8.11 AC-03, 14 Part 8, 4.2 |
| C-14 | Upload rejection statuses | 415 for an unsupported file type, 413 for an oversized upload. The error body also carries a machine-readable `code`. | Section 6.4 asks explicitly for statuses beyond 200, 201 and 400, and names unsupported file types and oversized uploads as two of the cases. Answering both with 400 leaves that requirement unanswered. | 6.4 |
| C-15 | Attachment upload flow | Two steps: `POST /api/tickets` creates the Ticket, then `POST /api/tickets/:id/attachments` uploads each file. On partial failure the Ticket is kept and the failed files are listed and retryable. | Section 4.5 asks what happens when a Ticket is created but attachment upload fails, and asks for a compensation strategy, both of which presume two steps. The same endpoint then serves the "add an attachment to an existing ticket" function on Ticket Detail. | 4.5 |
| C-16 | Upload dependency | `multer` is approved for multipart parsing. No other new runtime dependency is added without explicit approval. | Two-step upload requires multipart parsing and the server has no such dependency. `multer` is Express's standard middleware rather than a framework, so it does not disturb the mandatory stack. Base64 JSON would inflate payloads by a third and make the 5 MB limit ambiguous about which side of encoding it applies to. | 4.5 |
| C-17 | Attachment storage | Files are stored on disk under `server/uploads/` with a generated safe filename; the original filename is retained as metadata. `server/uploads/` is git-ignored. Files are served only through an ownership-checked download route, never as a static directory. | Section 4.5 requires safe filename and storage behavior and requires removed files never to be downloadable. Static serving would leave removed files fetchable by direct URL, breaking the one attachment rule the labsheet calls fixed. | 4.5 |
| C-18 | Attachment slot accounting | Soft-removed attachments do not count toward the five-per-Ticket maximum. | The labsheet limit is stated as five *active* attachments per Ticket. This also produces a clean boundary test: five active, remove one, upload succeeds. | 4.5 |
| C-19 | Removal reason | A removal reason is required, must be non-empty after trimming, and is stored alongside `removedAt`. | Section 4.5 leaves the requirement open, but Part 8 requires a screenshot of soft removal *with reason*. Making the field optional risks producing that screenshot with an empty value. | 4.5, 14 Part 8 |
| C-20 | Removed-file download | 410 Gone to the owning Requester. 403 to a non-owner, consistent with C-13. | The owner can already see the attachment metadata, so 410 is truthful and distinguishes a removed file from one that never existed. A non-owner is refused on ownership grounds before removal state is considered. | 4.5, 6.4 |
| C-21 | Current Status enum | The enum carries `NEW`, `IN_PROGRESS`, `RESOLVED`, `CLOSED`, `CANCELLED`. Only `NEW` is reachable in Lab 2. | Section 4.2 forbids status *transitions*, not a wider enum. My Tickets requires a status filter and status badges, and a filter over a single-valued column is a degenerate control. The wider enum costs nothing and is the natural Lab 3 seam. | 4.3 BR-02, 4.2, 8.4 |
| C-22 | Demo data | The graded seed stays exactly as Section 5.3 specifies. A separate, non-graded `server/prisma/seed-demo.ts` creates the Tickets needed for pagination and the Requester A/B demonstration. | Section 5.3's required seed contains no Tickets, yet Part 7 needs a paginated list, a contrasting Requester, an empty state and a no-results state. Keeping demo data out of the graded seed avoids any suggestion that the seed does not conform, while making Part 7 reproducible after a database reset. | 5.3, 14 Part 7 |
| C-23 | Requested Priority | Values are `LOW`, `MEDIUM`, `HIGH`. The field is required and the form defaults to `MEDIUM`. | `MEDIUM` is the only value the labsheet shows. Three levels keep the badge rules simple; adding `URGENT` invites the escalation semantics that Section 4.2 excludes as IT Staff workflow. | 4.4, 6.2 |
| C-24 | Related System scoping | Categories and Related Systems are independent flat lists. A Related System is not scoped to a Category. | Section 6 exposes them as two sibling "retrieve active" endpoints with no nesting, and the Section 5.1 relationships attach both directly to Ticket and never to each other. | 5.1, 6 |
| C-25 | Field validation | Values are trimmed before validation. Ticket Summary is required, 5-120 characters. Description is required, 20-5000 characters. Category, Related System and Requested Priority are required. Client and server validate identically and emit identical messages. | Section 4.4 requires the length constraints to be defined and justified and requires both frontend and backend validation. Fixed numeric bounds make the boundary tests that Section 9.2 mandates possible to write. | 4.4, 9.2 |
| C-26 | Duplicate submission | UI-level prevention only. The submit button is disabled while the request is in flight, as Section 8.3 already requires. There is no server-side duplicate guard. Covered by a UI test asserting that the button is disabled and that the API is called exactly once. | A time-window duplicate guard produces a time-dependent test, and the Definition of Done forbids flaky tests. Lab 2 has no authenticated caller to anchor an idempotency key to, so a server-side guard would key on request content alone and would reject legitimate near-identical submissions. | 4.3, 8.3, 13.1 |
| C-27 | Ticket-list query contract | `?search=&categoryId=&relatedSystemId=&currentStatus=&sort=createdAt:desc&page=1&pageSize=10`. Permitted page sizes are 10, 25 and 50. A malformed or unknown `sort` or `pageSize` returns 400 with field errors. A `page` beyond the last returns an empty `data` array with correct metadata rather than an error. Response shape is `{ data: [...], meta: { page, pageSize, total, totalPages, sort } }`. The status filter parameter was renamed from `status` to `currentStatus`, and the sort token in `specification.md` BR-25 renamed with it, for consistency with the C-40 glossary, which fixes the field name as `currentStatus`. It was the only query token that differed from its glossary name. | Section 6.1 lists exactly these decisions as required of the student. Returning an empty page rather than an error for an over-range page is what allows the empty state and the no-results state to be distinguished at the API level, per C-28. | 6.1 |
| C-28 | Empty vs no-results | Two distinct states. "No tickets yet" applies when the Requester has zero tickets and no filters are active, and offers a Create Ticket action. "No matches" applies when a search or filter is active, and offers Clear filters. | Appendix C requires empty-list and no-results presentation to be specified separately, and Part 7 screenshots them separately. They must therefore differ in copy and in the action offered, not merely in wording. | 4.3, 8.4, 17 Appendix C |
| C-29 | Sort order | Default sort is `createdAt DESC`, with `id DESC` as the secondary sort. | Section 6.1 requires a secondary sort to be defined. A deterministic tiebreak follows the Lab 1 precedent that list ordering is predictable and asserted in tests. | 6.1 |
| C-30 | Searchable fields | Search covers Ticket Number, by exact or prefix match, and Ticket Summary, case-insensitive contains. Description is not searched. | This keeps a defensible index justification for Section 5.2. Including Description would require a full-text discussion that carries no marks. | 6.1, 5.2 |
| C-31 | Requester model name | The Prisma model is `RequesterUser`. The UI label is "Development Requester". | Section 11.2 names `RequesterUser` when addressing the implementer, while Section 5 uses the descriptive term for the entity. Using both in their own registers keeps schema and screen consistent with the labsheet. | 5, 11.2 |
| C-32 | Selection storage | The selected Requester is held in React context and persisted in `localStorage`. On boot the stored id is re-validated against the active-requester list; if it is missing or now inactive it is cleared and the Selection screen is shown with an explanatory message. | Section 8.1 requires the storage mechanism to be defined. AC-02 - no Requester selected, therefore the Selection screen is shown - is only testable against a defined persisted state. This also resolves the inactive-Requester area of Section 4.3. | 8.1, 4.3, 8.11 AC-02 |
| C-33 | Inactive Requesters | An inactive Requester never appears in the selector. An API request naming an inactive Requester is rejected with 403 and a safe message. Existing data belonging to an inactive Requester is retained. | Section 5.3 requires at least one inactive Requester to be seeded, which exists precisely so this path can be demonstrated. | 5.3, 4.3 |
| C-34 | Ticket Date | Ticket Date is the system-generated `createdAt`. It is read-only and displayed, never entered. | Section 4.4 states that some values are system-generated or read-only, and a user-entered date carries no meaning for a support request. | 4.4 |
| C-35 | AI use document name | Lab 2 uses `docs/lab-02/ai-use.md` with a hyphen. The Lab 1 file `docs/lab-01/ai_use.md` keeps its underscore. | Each labsheet names its own file, and Section 12 and Part 4 both use the hyphen. Renaming the Lab 1 file would break links in the already-submitted Lab 1 report. | 12, 14 Part 4 |
| C-36 | Specification file name | The file is `docs/lab-02/specification.md`, singular. | Sections 4.3, 8.9 and 12 all use the singular. The plural appears once, in the Appendix A introduction, and is treated as a slip. | 12, 8.9, 15 |
| C-37 | Migration strategy | One additive migration introduces the Lab 2 models and the `isActive` columns. `prisma migrate reset` is never run. | A reset destroys the Lab 1 seed data in the `toktickit-db` container. An additive migration preserves the existing Category rows that the Lab 1 tests assert against. | 5, 5.2 |
| C-38 | Environment variables | `UPLOAD_DIR` and `MAX_UPLOAD_BYTES` are added to `server/.env.example` in the same commit as the upload code. No credentials or real values enter a tracked file. | Uploads introduce configuration that a reader must reproduce, and `.env.example` is Part 1 evidence. The Lab 1 secrets rule is unchanged. | 4.5, 14 Part 1 |
| C-39 | Timestamp handling | Timestamps are stored in UTC and displayed in Asia/Bangkok using the single fixed format `DD MMM YYYY HH:mm`. | Parts 6 and 7 screenshot dates, and Section 8.8 requires visual comparison against `ui-spec.md`. An unpinned display format makes that comparison unfalsifiable. | 14 Parts 6-7, 8.8 |
| C-40 | Terminology | A canonical glossary in `specification.md` Section 11 fixes each term once. Its spellings are used verbatim as `api-spec.md` field names and `ui-spec.md` labels. | The labsheet mixes "Ticket Summary" with "Summary" and "Current Status" with "status". Four cross-referencing documents plus a traceability matrix are where such drift becomes contradiction. | 8.10, 9.1 |
| C-41 | Product name | The product is **TokTickIT** everywhere - headings, page titles, component names, test assertions and documentation. The header rendered as "TikTockIT" in the screenshot on page 9 is treated as a typo in the illustration. | Section 8.1 requires a "TokTickIT title" and every body reference in the labsheet spells it TokTickIT. The Lab 1 codebase and its passing tests already assert TokTickIT. | 8.1, illustration p. 9 |
| C-42 | Identity transport, non-creation endpoints | `requesterId` travels as `?requesterId=` on every endpoint except `POST /api/tickets`, which keeps it in the JSON body. This covers `POST /api/tickets/:id/attachments` and `DELETE /api/attachments/:id`, which C-12 did not reach. | C-12 fixed only the GET endpoints and ticket creation, leaving the two remaining mutating endpoints undefined. One rule - query parameter everywhere except the single creation endpoint - is easier to test and to remember than a per-endpoint table, and it keeps the identifier out of a multipart body where it would sit oddly beside file parts. | 6, 6.3, C-12 |
| C-43 | Malformed `page` | A `page` that is not a positive integer is malformed input and returns 400, exactly as a bad `sort` or `pageSize` does. A `page` that parses but exceeds the last page still returns an empty `data` array with correct metadata, unchanged from C-27. | C-27 and BR-28 named only `sort` and `pageSize` as the malformed cases, leaving `page=abc` undefined. Treating it as malformed follows the general 400 rule and keeps the over-range case, which is a valid integer, distinct from the unparseable case. | 6.1, 6.4, C-27 |
| C-44 | Download and preview selection | `GET /api/attachments/:id/download` takes an optional `?disposition=inline\|attachment`, defaulting to `attachment`. Ownership and removal state are checked before `disposition` is considered, so an invalid disposition never changes which of 403, 404 or 410 a caller receives. | BR-49 fixes inline preview and attachment download as two behaviors of one ownership-checked route but not the mechanism that selects between them. Defaulting to `attachment` leaves the plain download URL unchanged, making preview the explicit request. Checking ownership first keeps the C-13 and C-20 status rules intact regardless of the parameter. | 4.5, BR-49, C-17, C-20 |
| C-45 | Unknown versus inactive Requester | A `requesterId` naming no Requester at all returns 404. A `requesterId` naming a Requester whose `isActive` is false returns 403. The Requester is resolved before the addressed Ticket or Attachment is loaded, so an unknown `requesterId` answers 404 without revealing whether the addressed resource exists. | C-13 already separates missing from forbidden, and applying the same separation to the caller keeps one rule across the contract instead of two. C-33's 403 is specifically about an inactive Requester, which is a refusal rather than an absence. | 6.4, C-13, C-33 |
| C-46 | Unavailable attachment state | The fifth attachment state named in `specification.md` section 6, **unavailable**, means the Attachment row exists and its metadata is visible, but its bytes could not be served: a 410 on an Attachment the current view believed active, or a 500 from the download route. It renders with its actions disabled, an explanatory note, and a Refresh action. | `specification.md` names five attachment states but fixes a condition for only four; `ui-spec.md` could not present a state with no trigger. 410 and 500 are the only two `api-spec.md` responses that leave a visible row unopenable, so they are the only conditions the state can have without inventing a new failure mode. | 8.5, 17, BR-50, BR-51 |
| C-47 | Selection screen screenshot location | The Development Requester Selection screen's four states are captured as `artifacts/lab-02/screenshots/create-ticket/selection-<viewport>-{loading,populated,empty,failure}.png`. No fourth screenshot folder is added. | Section 12 fixes exactly three screenshot folders and none is for the Selection screen, yet the Definition of Done requires its four states captured. Part 6 covers Create Mode and explicitly asks for the Selection screen, its dropdown, the selected-user display, the Change Requester action and its loading and failure states, so `create-ticket/` is where the grader already looks. | 12, 8.1, 14 Part 6, C-10 |
| C-48 | Selection screen test file | `client/tests/lab-02/RequesterSelection.test.tsx` is added to the Section 12 client test set. The four Section 12 client filenames are unchanged. | Section 12 is a stated minimum, as C-08 already established when adding the unit test file. The Development Requester Selection screen carries nine acceptance criteria - AC-02, AC-04 to AC-10, and AC-12 - and Section 12 names no file for it. Placing them in `CreateTicket.test.tsx` would make that filename inaccurate about its own contents, which is the same defect C-02 rejected when Section 9.1 disagreed with Section 12. | 12, 9.1, 8.1, C-03, C-08 |
| C-49 | Ticket Number assignment point | The Ticket Number is assigned inside one `prisma.$transaction`: the `Ticket` row is created first, which is what makes the autoincrement `id` available, then the same row is updated with `` `TKT-${createdAt year}-${String(id).padStart(6, "0")}` ``. `ticketNumber` is therefore nullable in the schema, because the column has no value between the create and the update, but it is never null outside the transaction. The column is `@unique`, so the index C-30 relies on for exact and prefix search is the one that constraint creates; no separate index is declared. There is no sequence table and no collision retry, because the `id` the digits come from is already unique. | C-11 fixes the format and says the digits are the autoincrement id, but not when that id becomes available - and it is not available until after the insert, so a single-statement create cannot produce it. Wrapping create-then-update in one transaction means no caller and no query ever observes a Ticket without its number, which is what lets BR-01 and AC-15 be stated without qualification. A retry loop would be answering a collision risk that deriving from the primary key has already removed. | 4.3 BR-01, C-11, C-30, 5.2 |
| C-50 | Ticket Number year versus displayed Ticket Date | The year segment of the Ticket Number is the **UTC** year of `createdAt`, while the displayed Ticket Date is rendered in Asia/Bangkok per C-39. A Ticket created between 17:00Z and 24:00Z on 31 December therefore carries a number whose year is one behind its displayed date, for example `TKT-2026-000123` shown with a Ticket Date of `01 Jan 2027 00:30`. This is accepted for Lab 2. | `tests.md` UNIT-03 pins the UTC behaviour - a Ticket created `2025-12-31T23:59Z` yields `TKT-2025-…` - and the number is an identifier, not a date: it must be stable and unique, and it never has to agree with a rendered timestamp. Deriving it from the stored UTC instant keeps it independent of the display timezone, which C-39 fixes only for presentation. Lab 3 may align the two if a requirement appears. | 4.3 BR-01, BR-04, BR-10, C-11, C-39, C-49 |
| C-51 | Client-side routing | Client routing is the History-API router in `client/src/router.tsx` - roughly sixty lines providing the current path, `navigate()` and a `<Link>` - rather than `react-router-dom`. Lab 3 may adopt a library if the route count grows. | `CLAUDE.md` forbids adding a dependency without approval, and the mandatory stack table names no router. Lab 2 has four screens and no nested or dynamic matching beyond a Ticket id, which is well inside what a few dozen lines cover; a library would be answering a routing problem this sprint does not have. Keeping it in one file makes a later swap a local change. | LS 3 stack table, 8, C-04 |

## Notes

- C-03 carries the client test path decision together with its `client/vite.config.ts`
  reason; it is not duplicated as a separate row.
- C-13 and C-20 are linked. A change to either must be reflected in the other.
- C-11's regex `^TKT-\d{4}-\d{6}$` is the assertion used by the unit test named in C-08.
- C-01 is discharged by the Lab 2 rewrite of `CLAUDE.md`.
- C-27's filter parameter was renamed `status` -> `currentStatus`; `specification.md` BR-25's
  sort token and `api-spec.md` were renamed with it.
- C-49 answers a review comment on C-11: the format was fixed but the assignment point
  was not. C-11 is unchanged; C-49 supplies the mechanism.
- C-50 was recorded during Issue #11 when the data-model tests made the UTC-year edge case
  visible. It changes no behaviour; it records why the year in the number and the year in
  the displayed date are allowed to differ on one evening a year.
- C-51 was recorded during Issue #12 when the Selection screen needed a route table and the
  stack had no router. It is the reason `client/src/router.tsx` exists.
- C-48 closes the last open choice from the Phase 1 documents; `tests.md` section 7
  now cites it rather than listing it as a candidate.
- C-46 and C-47 promote two choices that `ui-spec.md` had made locally; that document's
  section 18 now records them as settled rather than open.
- C-42 to C-45 resolve the four mechanism gaps raised while writing `api-spec.md`. Each was a
  place where the labsheet and the earlier decisions fixed a behavior but not the mechanism.
- C-45 supersedes the working assumption in an earlier `api-spec.md` draft that an unknown
  `requesterId` returned 403.
- C-22's demo seed path was corrected from `prisma/seed-demo.ts` to
  `server/prisma/seed-demo.ts` to match the repository layout, which places Prisma under
  `server/prisma/`. The decision itself is unchanged.
