// My Tickets — placeholder route target for the shell navigation.
// The list itself (search, filters, sort, pagination) lands with Issue #14.
export default function MyTickets() {
  return (
    <section aria-labelledby="my-tickets-title">
      <h1 id="my-tickets-title" className="tk-title">My Tickets</h1>
    </section>
  );
}
